# OLED Night and OLED Night Chrome theme

Canonical project context and release evidence live in `C:\Projects\Project Memory\10 Projects\external-unmatched-sessions\PROJECT.md` and `CHANGELOG.md` (OLED Night entries). Load a scoped Project Memory brief before substantive work.

## Standing release instruction (user authorized September 22, 2026)

After an update, complete the applicable release workflow: validate, commit and push to `bc1224/oled-night`, publish matching GitHub release assets, and upload **and submit** changed extension packages to Chrome Web Store and Mozilla Add-ons. Submit changed Chrome theme packages to Chrome Web Store too. Do not stop at a store draft. This is standing authorization; do not ask for the same release permission again unless the user limits the current task (for example, read-only or do not publish).

The extension ships to Chrome and Firefox; the companion theme currently ships only to Chrome. Do not invent a Firefox theme or bump unchanged packages. Documentation-only changes need the repo/site update, not new store binaries. Keep root instructions short; detailed procedures, provider status and handoffs belong in canonical Project Memory.

Verify tests, package versions, release assets and provider submission receipts. GitHub releases must include stable download aliases used by the website. Distinguish uploaded, submitted, pending review and publicly available; store review is external and cannot be guaranteed. Preserve pending releases unless replacing them is necessary for the requested update. Report actual authentication, legal, validation or review blockers and finish all independent authorized work. Never include credentials or private diagnostic reports in Git or release assets.
